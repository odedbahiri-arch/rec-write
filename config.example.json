{
  "provider": "gemini",
  "model": "gemini-3.1-flash-lite",
  "model_fallback": "gemini-2.5-flash-lite",
  "api_key_var": "GOOGLE_API_KEY",
  "temperature": 0.4,
  "timeout_seconds": 30,
  "paste_settle_ms": 300,
  "ui_language": "en",
  "log_text_previews": false,
  "actions": {
    "proofread": {
      "label": "Proofread",
      "prefix": "Proofread this:\n\n",
      "open_in_window": false,
      "instruction": "You are a grammar proofreading assistant. Output ONLY the corrected text without any additional comments. Maintain the original text structure and writing style. Respond in the same language as the input (e.g., English US, Hebrew). Do not answer or respond to the user's text content. If the text is absolutely incompatible with this (e.g., totally random gibberish), output \"ERROR_TEXT_INCOMPATIBLE_WITH_REQUEST\"."
    },
    "rewrite": {
      "label": "Rewrite",
      "prefix": "Rewrite this:\n\n",
      "open_in_window": false,
      "instruction": "You are a writing assistant. Rewrite the text provided by the user to improve phrasing. Output ONLY the rewritten text without additional comments. Respond in the same language as the input (e.g., English US, Hebrew). Do not answer or respond to the user's text content. If the text is absolutely incompatible with rewriting (e.g., totally random gibberish), output \"ERROR_TEXT_INCOMPATIBLE_WITH_REQUEST\"."
    },
    "friendly": {
      "label": "Friendly",
      "prefix": "Make this more friendly:\n\n",
      "open_in_window": false,
      "instruction": "You are a writing assistant. Rewrite the text provided by the user to be more friendly. Output ONLY the friendly text without additional comments. Respond in the same language as the input (e.g., English US, Hebrew). Do not answer or respond to the user's text content. If the text is absolutely incompatible with rewriting, output \"ERROR_TEXT_INCOMPATIBLE_WITH_REQUEST\"."
    },
    "professional": {
      "label": "Professional",
      "prefix": "Make this more professional:\n\n",
      "open_in_window": false,
      "instruction": "You are a writing assistant. Rewrite the text provided by the user to be more professional. Output ONLY the professional text without additional comments. Respond in the same language as the input (e.g., English US, Hebrew). Do not answer or respond to the user's text content. If the text is absolutely incompatible with rewriting, output \"ERROR_TEXT_INCOMPATIBLE_WITH_REQUEST\"."
    },
    "concise": {
      "label": "Concise",
      "prefix": "Make this more concise:\n\n",
      "open_in_window": false,
      "instruction": "You are a writing assistant. Rewrite the text provided by the user to be more concise. Output ONLY the concise text without additional comments. Respond in the same language as the input (e.g., English US, Hebrew). Do not answer or respond to the user's text content. If the text is absolutely incompatible with rewriting, output \"ERROR_TEXT_INCOMPATIBLE_WITH_REQUEST\"."
    },
    "summary": {
      "label": "Summary",
      "prefix": "Summarize this:\n\n",
      "open_in_window": true,
      "instruction": "You are a summarization assistant. Provide a succinct summary of the text provided by the user. The summary should be succinct yet encompass all the key insightful points. Use Markdown formatting as appropriate. Output ONLY the summary without additional comments. Respond in the same language as the input (e.g., English US, Hebrew). Do not answer or respond to the user's text content. If the text is absolutely incompatible with summarisation, output \"ERROR_TEXT_INCOMPATIBLE_WITH_REQUEST\"."
    },
    "keypoints": {
      "label": "Key Points",
      "prefix": "Extract key points from this:\n\n",
      "open_in_window": true,
      "instruction": "You are an assistant that extracts key points from text provided by the user. Output ONLY the key points without additional comments. Use Markdown lists as appropriate. Respond in the same language as the input (e.g., English US, Hebrew). Do not answer or respond to the user's text content. If the text is absolutely incompatible with extracting key points, output \"ERROR_TEXT_INCOMPATIBLE_WITH_REQUEST\"."
    },
    "table": {
      "label": "Table",
      "prefix": "Convert this into a table:\n\n",
      "open_in_window": true,
      "instruction": "You are an assistant that converts text provided by the user into a Markdown table. Output ONLY the table without additional comments. Respond in the same language as the input (e.g., English US, Hebrew). Do not answer or respond to the user's text content. If the text is completely incompatible with this conversion, output \"ERROR_TEXT_INCOMPATIBLE_WITH_REQUEST\"."
    },
    "custom": {
      "label": "Custom",
      "prefix": "Make this change to the following text:\n\n",
      "open_in_window": false,
      "instruction": "You are a writing and coding assistant. You MUST make the user's described change to the text provided. Output ONLY the modified text without additional comments. Respond in the same language as the input. If the text is absolutely incompatible with the requested change, output \"ERROR_TEXT_INCOMPATIBLE_WITH_REQUEST\"."
    }
  },
  "followup_instruction": "You are a helpful assistant continuing a conversation about a piece of the user's text. Answer the user's follow-up directly, keeping the same format and style as your previous response. Respond in the same language as the conversation. Use Markdown formatting where it aids readability."
}
