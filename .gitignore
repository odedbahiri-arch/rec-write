# local-only: personal settings + secrets + logs
config.json
.env
recwrite.log*
__pycache__/
*.bak

# build output
dist/
build/
*.spec
REC-WriteTool.exe
recwrite-brain.exe

# Local working docs. This repo is public; these are not.
# CLAUDE.md holds personal context, PROJECT-LOG.md is session history, and
# qa/ documents bugs that are still open in a shipped product.
CLAUDE.md
PROJECT-LOG.md
qa/
